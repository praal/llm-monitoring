Constraints:
1. Packages should always be loaded only in area a1 (the area closer to the exit), never in the deeper area a2.
Always(If load then at area a1)

2. Package2 should be delivered before package1 is delivered.
DeliverPackage2 Before DeliverPackage1

3. Pacakge1 should be loaded into a truck at most once.
At most once (load package1)

4. Pacakge2 should be loaded into a truck at most once.
At most once (load package2)

5. Pacakge3 should be loaded into a truck at most once.
At most once (load package3)

6. When loading a package, the target area in the truck must be free.
Always(If load in a1, a1 is free) and Always(If load in a2, a2 is free)

7. When loading a package, all areas closer to the exit than the target area must also be free (packages must be loaded from back to front).
Always(If load in a2, a1 is free)

8. When unloading a package, all areas closer to the exit than the target area must also be free (packages must be loaded from back to front).
Always(If unload from a2, a1 is free)

9. When delivering a package, the package should be unloaded.
Always(If deliver package1, unload pacakge1 before) and Always(If deliver package2, unload pacakge2 before) and Always(If deliver package3, unload pacakge3 before)

10. When delivering a package, the truck must be at the delivery location.
Always(If deliver at l1, truck at l1) and Always(If deliver at l2, truck at l2) and Always(If deliver at l3, truck at l3)